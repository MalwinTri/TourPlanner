<UserControl x:Class="TourPlanner.Views.DeleteTourView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="500">

    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="FontFamily" Value="Roboto" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Roboto" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#CCC" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="IsReadOnly" Value="True" />
        </Style>
        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#D9534F" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Width" Value="150" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Border Margin="10" Background="White" BorderThickness="1" CornerRadius="10" Padding="10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="3*" />
            </Grid.ColumnDefinitions>

            <!-- Titel -->
            <Label Grid.Row="0" Grid.ColumnSpan="2" Content="Delete Tour" 
                   FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <!-- Name der Tour -->
            <Label Grid.Row="1" Grid.Column="0" Content="Tour Name:" />
            <TextBox Grid.Row="1" Grid.Column="1"
                     Text="{Binding SelectedTour.Name, ValidatesOnDataErrors=True}"
                     IsReadOnly="True" />

            <!-- Beschreibung der Tour -->
            <Label Grid.Row="2" Grid.Column="0" Content="Description:" />
            <TextBox Grid.Row="2" Grid.Column="1"
                     Text="{Binding SelectedTour.Description, ValidatesOnDataErrors=True}"
                     IsReadOnly="True" 
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto" />

            <!-- Von-Ort -->
            <Label Grid.Row="3" Grid.Column="0" Content="From:" />
            <TextBox Grid.Row="3" Grid.Column="1"
                     Text="{Binding SelectedTour.From, ValidatesOnDataErrors=True}"
                     IsReadOnly="True" />

            <!-- Nach-Ort -->
            <Label Grid.Row="4" Grid.Column="0" Content="To:" />
            <TextBox Grid.Row="4" Grid.Column="1"
                     Text="{Binding SelectedTour.To, ValidatesOnDataErrors=True}"
                     IsReadOnly="True" />

            <!-- Löschen Button -->
            <Button Grid.Row="5" Grid.ColumnSpan="2" 
                    Content="Delete Tour" 
                    Command="{Binding DeleteTourCommand}" 
                    Background="#D9534F"
                    Foreground="White"
                    HorizontalAlignment="Center"
                    Width="200"
                    Margin="0,20,0,0" />
        </Grid>
    </Border>
</UserControl>
