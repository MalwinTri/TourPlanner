<UserControl x:Class="TourPlanner.Views.ToursBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="150"
             d:DesignWidth="600">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- Tour-Button -->
        <Button Command="{Binding ShowTourViewCommand}"
                Grid.Column="0"
                Margin="5">
            <Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border Background="#3399CC" CornerRadius="5">
                        <TextBlock Text="Tours"
                                   FontSize="20"
                                   FontWeight="Bold"
                                   Padding="10"
                                   Foreground="White"
                                   TextAlignment="Center" />
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>

        <!-- Weitere Buttons -->
        <StackPanel Grid.Column="1" Margin="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Content="Add Tour" 
                        Command="{Binding ShowAddTourViewCommand}"
                        Grid.Column="0" />

                <Button Content="Edit Tour" 
                        Command="{Binding EditTourCommand}"
                        Grid.Column="1" />

                <Button Content="Delete Tour" 
                        Command="{Binding DeleteTourCommand}"
                        Grid.Column="2" />

                <Button Content="Add Log"
                        Command="{Binding ShowAddLogViewCommand}"
                        Grid.Column="3" />

                <Button Content="Edit Log"
                        Command="{Binding EditLogCommand}"
                        Grid.Column="4" />

                <Button Content="Delete Log"
                        Command="{Binding DeleteLogCommand}"
                        Grid.Column="5" />
            </Grid>
        </StackPanel>
    </Grid>
</UserControl>
