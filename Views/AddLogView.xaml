<UserControl x:Class="TourPlanner.Views.AddLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="500">

    <UserControl.Resources>
        <!-- Stile für Labels, TextBox, Button, ComboBox, DatePicker -->
        <Style TargetType="Label">
            <Setter Property="FontFamily" Value="Roboto" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="FontFamily" Value="Roboto" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#CCC" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="Button">
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Background" Value="#3399CC" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Width" Value="150" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="FontFamily" Value="Roboto" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#CCC" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style TargetType="DatePicker">
            <Setter Property="FontFamily" Value="Roboto" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="BorderBrush" Value="#CCC" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </UserControl.Resources>

    <!-- Umrahmung -->
    <Border Margin="10"
            Background="White"
            BorderThickness="1"
            CornerRadius="10"
            Padding="10">
        <Grid>
            <!-- 7 Zeilen: 
                 0) Titel 
                 1) Date
                 2) Comment
                 3) TotalTime
                 4) Difficulty
                 5) Rating
                 6) Button
            -->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- 2 Spalten: Label, Eingabe -->
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Titel -->
            <Label Grid.Row="0" Grid.ColumnSpan="2"
                   Content="Add Log"
                   FontSize="18"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

            <!-- Date -->
            <Label Grid.Row="1" Grid.Column="0" Content="Date:" />
            <DatePicker Grid.Row="1" Grid.Column="1"
                        SelectedDate="{Binding DateTime, Mode=TwoWay, ValidatesOnDataErrors=True}"
                        ToolTip="Select a date for the log" />

            <!-- Comment -->
            <Label Grid.Row="2" Grid.Column="0" Content="Comment:" />
            <TextBox Grid.Row="2" Grid.Column="1"
                     VerticalScrollBarVisibility="Auto"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     Text="{Binding Comment, Mode=TwoWay, ValidatesOnDataErrors=True}"
                     ToolTip="Enter a comment for the log" />

            <!-- Total Time -->
            <Label Grid.Row="3" Grid.Column="0" Content="Total Time (hrs):" />
            <TextBox Grid.Row="3" Grid.Column="1"
                     Text="{Binding TotalTime, Mode=TwoWay, ValidatesOnDataErrors=True}"
                     PreviewTextInput="NumericOnly"
                     ToolTip="Enter the total time in hours (numbers only)" />

            <!-- Difficulty -->
            <Label Grid.Row="4" Grid.Column="0" Content="Difficulty:" />
            <ComboBox Grid.Row="4" Grid.Column="1"
                      SelectedItem="{Binding Difficulty, Mode=TwoWay, ValidatesOnDataErrors=True}"
                      ToolTip="Select the difficulty level">
                <ComboBoxItem Content="Easy" />
                <ComboBoxItem Content="Medium" />
                <ComboBoxItem Content="Hard" />
            </ComboBox>

            <!-- Rating -->
            <Label Grid.Row="5" Grid.Column="0" Content="Rating:" />
            <ComboBox Grid.Row="5" Grid.Column="1"
                      SelectedItem="{Binding Rating, Mode=TwoWay, ValidatesOnDataErrors=True}"
                      ToolTip="Rate the log from 1 to 5">
                <ComboBoxItem Content="1" />
                <ComboBoxItem Content="2" />
                <ComboBoxItem Content="3" />
                <ComboBoxItem Content="4" />
                <ComboBoxItem Content="5" />
            </ComboBox>

            <!-- Add Button -->
            <Button Grid.Row="6" Grid.ColumnSpan="2"
                    Content="Add Log"
                    Command="{Binding AddLogCommand}"
                    HorizontalAlignment="Center"
                    ToolTip="Click to add the log" />
        </Grid>
    </Border>
</UserControl>
