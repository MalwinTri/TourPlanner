<UserControl x:Class="TourPlanner.Views.TitleBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlanner.Views"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">

    <Grid DockPanel.Dock="Top" Background="#58729e">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>


        <StackPanel Orientation="Horizontal" Grid.Column="1" VerticalAlignment="Center" Margin="10">
            <Border CornerRadius="10,0,0,10" Background="White" Margin="5,0,0,0" BorderBrush="Black" BorderThickness="1">
                <TextBox Width="300" 
                         FontSize="18"
                         Background="Transparent" 
                         Foreground="Black" 
                         BorderThickness="0" 
                         x:Name="SearchTextBox" 
                         VerticalAlignment="Center"
                         />
            </Border>
            <Border CornerRadius="0,10,10,0" Background="#58729e" Margin="0,0,5,0" BorderBrush="Black" BorderThickness="1">
                <Button Content="🔍" 
                        Background="Transparent" 
                        Foreground="Black" 
                        BorderThickness="0" 
                        />
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
