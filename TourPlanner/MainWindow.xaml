<Window x:Class="TourPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:views="clr-namespace:TourPlanner.Views"
        Title="Tour Planner"
        Width="1200" Height="800"
        Background="#F0F8FF">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="VisibleIfTrueConverter" />

        <Style TargetType="Button">
            <Setter Property="Background" Value="#3399CC" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="8,4" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <DockPanel>

        <!-- Menu -->
        <Menu DockPanel.Dock="Top" Background="AliceBlue" Foreground="Black">
            <MenuItem Header="_File">
                <MenuItem Header="_Import" Command="{Binding ImportCommand}"/>
                <MenuItem Header="_Export" Command="{Binding ExportCommand}"/>
                <Separator />
                <MenuItem Header="_Report" Command="{Binding ReportCommand}"/>
                <MenuItem Header="_Summary" Command="{Binding SummaryCommand}" />
            </MenuItem>
            <MenuItem Header="_Edit">
                <MenuItem Header="Add Tour" Command="{Binding ShowAddTourDialog}"/>
                <MenuItem Header="Edit Tour" Command="{Binding ShowEditTourDialog}"/>
                <MenuItem Header="Delete Tour" Command="{Binding DeleteTour}"/>
            </MenuItem>
            <MenuItem Header="_Options">
                <MenuItem Header="Settings" Command="{Binding ShowSettingsCommand}" />
                <MenuItem Header="About" Command="{Binding ShowAboutCommand}" />
            </MenuItem>
        </Menu>

        <!-- Main Grid -->
        <Grid Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"/>
                <!-- Left Menu -->
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="300"/>
                <!-- Tour List -->
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="*"/>
                <!-- Preview -->
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <!-- Search -->
                <RowDefinition Height="*"/>
                <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Search Bar -->
            <views:SearchView Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="3" Margin="0,0,0,10"/>

            <!-- Left Menu -->
            <Border Grid.Row="1" Grid.Column="0" Background="#EAF6FF" CornerRadius="8" Padding="10">
                <views:TourMenuView />
            </Border>

            <!-- Center Tour List -->
            <Border Grid.Row="1" Grid.Column="2" Background="White" BorderBrush="#D3D3D3" BorderThickness="1" CornerRadius="8" Padding="10">
                <views:TourListView />
            </Border>

            <!-- Right Preview -->
            <Border Grid.Row="1" Grid.Column="4" Background="White" BorderBrush="#D3D3D3" BorderThickness="1" CornerRadius="8" Padding="10">
                <views:TourPreviewView />
            </Border>
        </Grid>

        <!-- Busy Overlay -->
        <Grid Background="#7FFFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource VisibleIfTrueConverter}}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Border Background="WhiteSmoke" Padding="10">
                <StackPanel>
                    <Label Content="{Binding IsBusyText}" Margin="5"/>
                    <ProgressBar IsIndeterminate="True" Margin="5,0,5,5"/>
                </StackPanel>
            </Border>
        </Grid>

    </DockPanel>
</Window>
