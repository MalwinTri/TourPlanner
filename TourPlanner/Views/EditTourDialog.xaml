<Window x:Class="TourPlanner.Views.EditTourDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Edit Tour" Height="450" Width="350" Background="#f0f6fc">
    <Window.Resources>
        <!-- TextBox + ComboBox Styles -->
        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="5"/>
        </Style>
        <!-- Button Style -->
        <Style TargetType="Button">
            <Setter Property="Margin" Value="10,20"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#3399cc"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2a8bbf"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <StackPanel>
            <!-- Header -->
            <Border Background="#dde6f1" CornerRadius="5" Padding="10" Margin="10">
                <Label Content="Edit Tour" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center"/>
            </Border>

            <!-- Input Fields -->
            <Label Content="Name:"/>
            <TextBox Text="{Binding Name}" />

            <Label Content="Tour Description:"/>
            <TextBox Text="{Binding Description}" />

            <Label Content="From:"/>
            <TextBox Text="{Binding From}" />

            <Label Content="To:"/>
            <TextBox Text="{Binding To}" />

            <Label Content="Transport Type:"/>
            <ComboBox SelectedValue="{Binding Transport}">
                <ComboBoxItem Content="Car" />
                <ComboBoxItem Content="Walking" />
                <ComboBoxItem Content="Bike" />
            </ComboBox>

            <!-- Save Button -->
            <Button Content="Save Changes" Command="{Binding EditTourCommand}" HorizontalAlignment="Center" Width="150"/>
        </StackPanel>
    </Grid>
</Window>
