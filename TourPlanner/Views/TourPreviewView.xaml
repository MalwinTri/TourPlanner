<UserControl x:Class="TourPlanner.Views.TourPreviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:converter="clr-namespace:TourPlanner.Helper"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="AliceBlue">

    <UserControl.Resources>
        <!-- Deine Button-Designs -->
        <Style TargetType="Button">
            <Setter Property="Background" Value="#3399CC" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="10 5" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Pfad-Konverter für Bilder -->
        <converter:PathConverter x:Key="PathConverter" />
    </UserControl.Resources>

    <Grid Margin="10">
        <Border Background="White" CornerRadius="10" Padding="15" BorderBrush="#3399CC" BorderThickness="1">
            <StackPanel>
                <TextBlock Text="Tour Details" FontWeight="Bold" FontSize="18" Foreground="#3399CC" TextAlignment="Center" Margin="0 0 0 10"/>

                <StackPanel DataContext="{Binding SelectedTour}">
                    <!-- Bild der Tour -->
                    <Image Height="200"
                           Source="{Binding Path=ImagePath, Converter={StaticResource PathConverter}, FallbackValue='https://via.placeholder.com/300x200.png'}"
                           Margin="0 0 0 15"
                           Stretch="UniformToFill"
                           HorizontalAlignment="Center"
                           />

                    <!-- Tour Info -->
                    <StackPanel Orientation="Vertical" Margin="5">
                        <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" TextAlignment="Center" Foreground="#333"/>
                        <TextBlock Text="{Binding From, StringFormat=From: {0}}" FontSize="14" Margin="0,5,0,0"/>
                        <TextBlock Text="{Binding To, StringFormat=To: {0}}" FontSize="14"/>
                        <TextBlock Text="{Binding Transport, StringFormat=Transport: {0}}" FontSize="14"/>
                        <TextBlock Text="{Binding Description, StringFormat=Description: {0}}" FontSize="14" TextWrapping="Wrap"/>
                    </StackPanel>
                </StackPanel>

                <!-- Button -->
                <Button Content="Open Details" Command="{Binding DetailsCommand}" HorizontalAlignment="Center" Width="120" Margin="0 15 0 0"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
