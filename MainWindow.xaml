<Window x:Class="TourPlanner.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:TourPlanner.Views"
        mc:Ignorable="d"
        Title="Tour Planner"
        Height="800"
        Width="1200">

    <Window.Resources>
        <!-- Standard-Style für Buttons -->
        <Style TargetType="{x:Type Button}">
            <Setter Property="Background" Value="#3399CC" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="10 5" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type Button}">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter
                                HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Converter für Busy/Loading-Indikator -->
        <BooleanToVisibilityConverter x:Key="VisibleIfTrueConverter" />
    </Window.Resources>

    <ScrollViewer>
        <Grid ShowGridLines="False">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="47*" />
                <ColumnDefinition Width="1136*" />
            </Grid.ColumnDefinitions>

            <Grid Margin="5,5,5,5" Grid.ColumnSpan="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition />
                    <!-- Hier wird z. B. ContentControl eingefügt -->
                </Grid.RowDefinitions>

                <!-- 1) Titel -->
                <TextBlock Grid.Row="0"
                           Text="Tour Planner"
                           FontSize="20"
                           FontWeight="Bold"
                           Margin="10,10,0,0"
                           FontFamily="Roboto" />

                <!-- 2) Trennlinie -->
                <Rectangle Grid.Row="1"
                           Height="1"
                           Fill="Gray"
                           Margin="0,0,0,5" />

                <!-- 3) NavBar -->
                <views:NavBarView x:Name="NavBarView"
                                  Grid.Row="2" />

                <!-- 4) Suchleiste -->
                <views:SearchView x:Name="SearchView"
                                  Grid.Row="3" />

                <!-- 5) NEUE UserControl für die Touren-Buttons / "ToursBarView" -->
                <views:ToursBarView x:Name="ToursBarView"
                                    Grid.Row="4" />

                <!-- 6) ContentControl, z. B. wenn andere Views/Seiten angezeigt werden sollen -->
                <ContentControl Content="{Binding CurrentView}"
                                Grid.Row="5" />
            </Grid>

            <!-- Loading-/Busy-Overlay -->
            <Grid Background="#7FFFFFFF"
                  Visibility="{Binding IsBusy, Converter={StaticResource VisibleIfTrueConverter}}"
                  Grid.ColumnSpan="2">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="64*" />
                    <ColumnDefinition Width="22*" />
                    <ColumnDefinition Width="505*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="592*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

            </Grid>
        </Grid>
    </ScrollViewer>
</Window>
